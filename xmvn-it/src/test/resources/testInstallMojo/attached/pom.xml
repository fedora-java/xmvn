<project>
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>xmvn.it.install.mojo</groupId>
    <artifactId>install-parent</artifactId>
    <version>42</version>
    <relativePath>..</relativePath>
  </parent>
  <artifactId>install-attached</artifactId>
  <dependencies>
    <dependency>
      <groupId>${project.groupId}</groupId>
      <artifactId>install-simple</artifactId>
      <version>42</version>
    </dependency>
  </dependencies>
  <build>
    <plugins>
      <plugin>
	<artifactId>maven-jar-plugin</artifactId>
	<executions>
	  <execution>
	    <goals>
	      <goal>test-jar</goal>
	    </goals>
	  </execution>
	</executions>
      </plugin>
    </plugins>
  </build>
</project>
